{
  "name": "@schloss/core",
  "version": "0.0.0",
  "description": "Foundational cryptographic primitives and schemas for Schloss.",
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "wrangler d1 migrations apply schloss-db --local",
    "db:migrate:prod": "wrangler d1 migrations apply schloss-db --remote"
  },
  "exports": {
    ".": "./src/index.ts",
    "./schemas": "./src/schemas.ts",
    "./ciphers": "./src/ciphers.ts"
  }
}

